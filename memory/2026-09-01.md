# ARSKQ 每日进化 2026-09-01

## 新闻采集
来源：The Verge（news.google.com、MIT Tech Review RSS 因网络超时不可用，改用 The Verge 主页采集）。

新增 3 条新闻到 `src/services/ethicsNewsService.js` 的 `_fallbackNews` 数组最前：
1. OpenAI 测试"赞助智能体"广告（TIME 报道）— 数字公平 / 广告披露
2. Google 寻求迪士尼、环球等片厂授权角色与电影用于 AI 工具 — 版权 / 创作者权益 / 岗位流失
3. ARIA 将 wholly AI-generated 音乐排除出澳大利亚排行榜 — AI 署名与检测盲区

## 题库
新增 1 道题（id: 132，dimension: digital_fairness）：
- 主题：AI 助手中的"赞助智能体"广告与披露

`node --check` 验证通过（bbqQuestions.js 与 ethicsNewsService.js 均 OK）。

## 构建部署
- `npm run build` 成功（built in 1.11s）
- 部署至 /var/www/html/ark（www-data 权限），DEPLOY OK

## Git
- 本地 commit 成功：`chore: daily 2026-09-01`（2 files changed, 37 insertions）
- `git push origin master` 失败（github.com 网络/TLS 连接问题，GnuTLS recv error -110，持续超时被 kill）
- 环境性网络问题，非代码问题。push 失败不影响本地部署。

## 备注
- git pull --rebase 同样因 TLS 错误失败（脚本已 `|| true` 容忍）。
- 网络问题建议后续排查（可能与 github.com 访问受限相关）。
